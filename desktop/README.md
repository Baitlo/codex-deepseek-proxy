# Desktop App

This directory contains a Tauri desktop app for codex-deepseek-proxy.

It provides a simple UI to save a DeepSeek API key, choose `deepseek-v4-pro` or `deepseek-v4-flash`, start the local proxy, configure Codex, and enable launch at login.
